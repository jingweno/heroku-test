{
  "name": "Go Test",
  "scripts": {
    "postdeploy": "echo `go run main.go` | curl -d @- http://requestb.in/1boam5g1"
  },
  "env": {
    "BUILDPACK_URL": "https://github.com/kr/heroku-buildpack-go/"
  }
}
